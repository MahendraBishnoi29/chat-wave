import { FC } from "react";

interface GroupChatModalProps {
  isOpen?: boolean;
  onClose: () => void;
}

const GroupChatModal: FC<GroupChatModalProps> = ({ isOpen, onClose }) => {
  return <div></div>;
};

export default GroupChatModal;
